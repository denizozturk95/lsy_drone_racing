"""v16 core: v11's tunnel MPCC path view with the v10.6 smoothed plan's parity caps applied.

Only the path view (arc_path.CappedTunnelArcPath) and settings differ from v11; the OCP/solver
are v11's (reused via controller_core_v11). REQUIRES the acados environment.
"""
